import { Container } from "@mui/system";
import styled from "styled-components";

const Card = (movieData) => {
  return (
    <Container>
    <div>Card</div>
    </Container>
  );
};

export default Card;